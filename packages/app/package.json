{
  "name": "robots-are-here",
  "productName": "Robots are here",
  "version": "1.0.0",
  "description": "A TypeScript Electron application",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc && tsc -p tsconfig.renderer.json && mkdir -p dist/renderer && cp src/renderer/index.html dist/renderer/ && mkdir -p dist/node_modules/tailwindcss && cp node_modules/tailwindcss/index.css dist/node_modules/tailwindcss/",
    "dev": "tsc && tsc -p tsconfig.renderer.json && mkdir -p dist/renderer && cp src/renderer/index.html dist/renderer/ && mkdir -p dist/node_modules/tailwindcss && cp node_modules/tailwindcss/index.css dist/node_modules/tailwindcss/ && electron dist/main.js --no-sandbox",
    "start": "electron dist/main.js --no-sandbox",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "data-model": "workspace:*",
    "vue": "^3.5.18",
    "vue-router": "^4.5.1"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "@vitejs/plugin-vue": "^6.0.0",
    "autoprefixer": "^10.4.21",
    "electron": "^33.0.2",
    "postcss": "^8.5.6",
    "tailwindcss": "^4.1.11",
    "typescript": "^5.6.3",
    "vite": "^7.0.6"
  }
}