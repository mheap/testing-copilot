{
  "name": "robots-are-here",
  "productName": "Robots are here",
  "version": "1.0.0",
  "description": "A TypeScript Electron application",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc && mkdir -p dist/renderer && cp src/renderer/index.html dist/renderer/",
    "dev": "tsc && mkdir -p dist/renderer && cp src/renderer/index.html dist/renderer/ && electron dist/main.js",
    "start": "electron dist/main.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "data-model": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "electron": "^33.0.2",
    "typescript": "^5.6.3"
  }
}