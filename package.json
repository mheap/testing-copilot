{
  "name": "robots-are-here-workspace",
  "version": "1.0.0",
  "description": "A TypeScript Electron application workspace",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter app dev",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.8",
    "typescript": "^5.6.3"
  },
  "packageManager": "pnpm@10.13.1"
}